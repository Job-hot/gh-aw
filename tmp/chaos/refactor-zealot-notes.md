# Refactor Notes (chaos test)

Tracking small naming improvements across the codebase.

- rename: `compileOpts` to `compileOptions` (readability)
- rename: `cfg` to `config` in local scopes
- rename: `mgr` to `manager` in workflow pkg
